var mongoose = require('mongoose');
/*
var Schema = mongoose.Schema;

var usersSchema = new Schema({
  firstName: String
});

var users = mongoose.model('users', usersSchema);
*/


//to be replaced
//var users = mongoose.model('users', {firstName: String});
